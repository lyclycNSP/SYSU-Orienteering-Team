{
  "name": "sysu-orienteering-team",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "markdown-it": "15.0.1",
    "yaml": "2.9.0"
  }
}
